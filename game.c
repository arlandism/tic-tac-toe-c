#include "game.h"
#include <stdio.h>
#include <stddef.h>

void Game(char user_token) {
  char board[9];
  size_t counter;
  for (size_t i = 0; i < 10; i++) {
    board[i] = '\0';
  }

  puts("I'm working!");
}

int select_move(char *board) {
  puts("Select a space");
  for (size_t i = 0; i < len(open_spaces); i++) {

  }
}
